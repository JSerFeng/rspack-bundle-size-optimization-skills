interface:
  display_name: "Rspack Bundle Optimization"
  short_description: "Agent-led bundle analysis from captured compiler facts"
  default_prompt: "Use $rspack-bundle-optimization. Let bundled JavaScript capture and measure factual data only; perform all opportunity discovery, semantic analysis, prioritization, risk review, changes, and conclusions yourself as the agent. Apply project edits only when my request explicitly asks to optimize, reduce, fix, apply, or implement."
