interface:
  display_name: "Rspack Bundle Optimization"
  short_description: "Audit bundles; optimize only when explicitly requested"
  default_prompt: "Use $rspack-bundle-optimization to exhaustively audit this project's production bundle without project edits, resolve every candidate into the persisted ledger, and continue to a measured fixed point. Apply changes only if my request explicitly asks to optimize, reduce, fix, apply, or implement."
