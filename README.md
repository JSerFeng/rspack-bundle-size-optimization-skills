# Rspack Bundle Size Optimization Skill

A skill for exhaustive, evidence-driven bundle audits and explicitly requested optimization in Rspack-family builds (`Rspack`, `Rsbuild`, and `Rspeedy`). It resolves every supported route and candidate, separates diagnostic potential from production-measured savings, and repeats from a fresh baseline until it reaches a fixed point.

## What it does

Analysis, investigation, review, and report requests run all ten checks in audit-only mode without project edits. Requests that explicitly ask to reduce, optimize, fix, apply, or implement enter optimize mode and may promote fully validated changes.

| Check | Question it answers |
| --- | --- |
| Production baseline and resolved config | Where are emitted raw/gzip bytes concentrated, and which size-related optimization options did the real production compilation use? |
| Chunk-group reachability | Does a chunk group carry JavaScript unreachable from its roots? |
| Retained unused modules | Which emitted `usedExports=[]` modules remain, and why? |
| Side-effects audit and A/B | After complete source and package review, which candidates are safe to test with `sideEffects:false`? |
| Export Usage Roots | Which exports are genuinely referenced, conservatively retained, or caused by generated artifacts? |
| Rollup-vs-Rspack diff | Which exports can Rollup remove that Rspack retains, and what source pattern causes the gap? |
| CJS-to-ESM | Would safely transforming eligible transpiled CommonJS improve shaking or concatenation? |
| splitChunks | Can a measured cache-group change reduce per-page load without hidden duplication? |
| ECMA target | Does raising transform and minifier targets reduce generated bytes? |
| Post-loader quality | Is the captured source readable and complete enough to support the conclusions? |

Each check ends as `completed`, `completed-no-op`, or `blocked`. Missing tools and incompatible APIs are reported as blocked with an exact unblock action; they are never disguised as “no candidates.”

Data capture is not completion. Every candidate must receive a source-backed terminal disposition in the persisted run ledger. In optimize mode, safe measured candidates are applied and validated automatically; in audit-only mode they remain recorded as `validated-opportunity` because edits were not authorized. Risk-bearing candidates remain unapplied with a concrete failure mode and clearing condition.

## Safety and measurement rules

- Rank and headline results by emitted JavaScript raw bytes; show gzip second.
- Keep confirmed production savings separate from source-size sums, diagnostic upper bounds, and estimated attribution.
- Run experiments from an isolated project-local audit directory with a manifest, a validated candidate ledger, and fresh artifact IDs.
- Capture effective optimization values from `compilation.options`; do not infer them from author-written config.
- Change one variable at a time between baseline and experiment.
- Never infer `sideEffects:false` from filenames, ESM syntax, `node_modules`, or `usedExports=[]`.
- Read each side-effects candidate's complete source, post-loader source, nearest package metadata, and entry relationships before deciding whether it is safe to experiment.
- Count only the production A/B emitted-byte delta as a side-effects saving.

## Interactive HTML report

The report includes:

- summary metrics and the measurement contract;
- coverage state, terminal candidate counts, and evidence for all ten checks, including the resolved optimization-config review;
- confirmed savings, optimization potential, blocked work, and an action queue;
- a searchable, sortable module list ordered by unused bytes by default;
- selected-module source drill-down with exact red highlighting and search navigation;
- lazy data shards, list/source virtualization, bounded caching, cancellation, and a local-only server for large reports;
- responsive layouts, keyboard controls, stale-artifact rejection, and privacy guidance.

## Requirements

- A skill-compatible coding environment with local filesystem and shell access.
- A Rspack, Rsbuild, or Rspeedy project with a production build command.
- Some checks use Rollup, `@swc/core`, and `@jridgewell/trace-mapping`. The skill performs a capability preflight and reports unavailable checks precisely instead of modifying the project's dependency manifest without permission.
- Export Usage Roots uses Rspack's built-in export-usage graph when available. `experiments.RsdoctorPlugin({ exportUsageGraph: true })` requires `@rspack/core >= 2.1.0`; older versions need a compatible development binding or `@rsdoctor/rspack-plugin`.

## Install

### Install with the Skills CLI (recommended)

Install into the current project:

```bash
npx skills add JSerFeng/rspack-bundle-size-optimization-skills --skill rspack-bundle-optimization
```

To make the skill available across projects:

```bash
npx skills add JSerFeng/rspack-bundle-size-optimization-skills --skill rspack-bundle-optimization --global
```

### Install manually

Copy or symlink the skill folder into the skills directory used by your coding environment. Prefer a symlink while authoring so the repository remains the single source of truth:

```bash
git clone https://github.com/JSerFeng/rspack-bundle-size-optimization-skills.git
skill_install_dir=/path/to/your/skills
ln -s "$(pwd)/rspack-bundle-size-optimization-skills/rspack-bundle-optimization" "$skill_install_dir/rspack-bundle-optimization"
```

The exact directory and discovery mechanism depend on the environment.

## Use

In a Rspack-family project, invoke the skill or ask your coding environment, for example:

- “Analyze and reduce my Rspack bundle size.”
- “Run a complete bundle-size audit and give me the HTML report.”
- “Find Rspack optimization gaps responsible for this production bundle's size.”

The skill inspects the project, creates an isolated run, executes every supported analysis route, validates the generated report in a browser, and returns the local report path or server URL.

## Layout

```text
rspack-bundle-optimization/
├── SKILL.md
├── agents/openai.yaml
├── references/
│   ├── analysis-00-baseline-config.md
│   ├── analysis-01-reachability.md
│   ├── analysis-02-retained-unused.md
│   ├── analysis-03-side-effects.md
│   ├── analysis-04-export-usage.md
│   ├── analysis-05-rollup-diff.md
│   ├── analysis-06-cjs-to-esm.md
│   ├── analysis-07-splitchunks.md
│   ├── analysis-08-ecma.md
│   ├── analysis-09-post-loader.md
│   ├── html-report-design.md
│   └── optimization-summary-template.md
└── scripts/
    ├── create-audit-run.cjs
    ├── optimization-config-check-plugin.template.cjs
    ├── side-effects-review-worklist.cjs
    ├── rollup-graph-capture-plugin.cjs
    ├── run-rollup-export-diff.cjs
    ├── transpiled-cjs-to-esm-loader.cjs
    ├── ecma-compilation-tree-capture-plugin.template.cjs
    ├── ecma-module-capture-plugin.template.cjs
    ├── ecma-module-diff.cjs
    ├── sourcemap-generated-byte-attribution.cjs
    ├── render-bundle-report.cjs
    ├── serve-bundle-report.cjs
    └── capture and analysis helpers
```

## License

[MIT](LICENSE).
